[project]
name = "tradeforge-schema"
version = "0.1.0"
description = "Strategy DSL: JSON Schema (source of truth) plus Python and TypeScript validators."
requires-python = ">=3.12,<3.13"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tradeforge_schema"]

{
  "schema_version": "1.0",
  "name": "Banda referenciada sem dizer qual",
  "description": "Bollinger responde com três valores, então `bb` sozinho é uma pergunta com três respostas. Não existe componente padrão de propósito: `bb` e `bb.middle` seriam duas grafias do mesmo valor, e a média é só a mais tentadora das três",
  "timeframe": "H1",
  "indicators": [{ "id": "bb", "type": "BOLLINGER", "params": { "period": 20 } }],
  "entry": {
    "long": {
      "op": "gt",
      "left": { "ref": "price.close" },
      "right": { "ref": "bb" }
    },
    "short": null
  },
  "exit": {
    "stop_loss": { "type": "candle_extreme", "params": { "lookback": 1, "side": "low" } }
  },
  "risk": { "sizing": { "type": "percent_risk", "params": { "percent": 1.0 } } }
}

[project]
name = "tradeforge-engine"
version = "0.1.0"
description = "Event-driven strategy engine: deterministic, lookahead-free, broker-agnostic."
requires-python = ">=3.12,<3.13"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tradeforge_engine"]
